server:
  port: 8081
spring:
  application:
    name: '@name@'
eureka:
  client:
    serviceUrl:
      defaultZone: http://${REGISTRY_SERVICE_HOST:localhost}:${RGISTRY_SERVICE_PORT:8761}/eureka
    enabled: false
